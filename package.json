{
  "name": "livkors-ecommerce",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "export": "prisma generate && next build",
    "export-cpanel": "npm run export && echo 'Build completed! Upload the dist/ folder to your cPanel public_html directory'",
    "start": "next start",
    "lint": "next lint",
    "seed": "node scripts/seed.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",
    "db:reset": "prisma migrate reset",
    "postinstall": "echo 'Skipping prisma generate for now'"
  },
  "dependencies": {
    "@headlessui/react": "^2.2.4",
    "@prisma/client": "^6.8.2",
    "@vercel/speed-insights": "^1.2.0",
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "lucide-react": "^0.511.0",
    "next": "15.3.2",
    "next-auth": "^4.24.11",
    "prisma": "^6.8.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}
